// Callabe - чтобы позволить екземпляру класса вызываться как функция 

class CallableFunction {
  String call(String a, String b, String c) => '$a $b $c';
}

var wf = CallableFunction();
var out = wf('Code', 'and', 'art');

main() async {
  print(out); //Code and art
}
